# Receipt — Step 1.1: Scanner proof policy and evidence states

**Outcome:** Added [docs/binary-trust.md](../../../docs/binary-trust.md) defining DëvSec's binary proof policy.

## Acceptance criteria

- [x] Policy distinguishes user-owned `PATH` tools from DëvSec-managed tools.
- [x] Names the proof levels: `unverified`, `checksum-pinned`, `upstream-signed`, `provenance-verified`, `devsec-signed`, plus the `user-owned` surfacing label.
- [x] States which proof levels execute by default for managed tools (`checksum-pinned`+ at install, plus a pre-execution integrity gate).
- [x] Explains that signing proves origin, not harmless behavior (dedicated section).
- [x] Notes legacy-install backward-compatible behavior.

## Open question surfaced

- Managed surface is **4 tools** (Gitleaks, Trivy, Syft, Grype), not 1. The README's "What's real" table still says gitleaks is "the first managed-install proof… broader managed installs deferred" — stale, should be corrected when 2.3 touches user-facing copy. The doc states the honest scope: "verifies what it runs" = the scanners DëvSec installs itself, not user-owned `PATH` tools.

## Follow-up code surfaces

**Step 1.2 (pre-execution integrity):**
- `src/security_observatory/managed_tools.py` — `install_managed_tool_files` records archive sha256 only (`checksum`), not the extracted binary's digest. Add binary digest to record `metadata`. Add a resolver that returns `none`/`ok`/`tampered`.
- `src/security_observatory/scanners.py:160` `_verified_managed_binary_for_scanner` / `run_scanner` — the swap-in point; a tampered managed binary must produce a visible skip, not a silent `PATH` fallback (current `return None` falls back to `shutil.which`).
- `manifest_record_from_db_record` must carry `metadata` for the manifest-fallback path.

**Phase 2 (verified downloads):**
- `MANAGED_INSTALL_PROOF_TARGETS` — add per-tool verification config.
- New `src/security_observatory/verification.py` — provider contract.
- Surfacing: `cli.py` `doctor` (~614), `catalog.py` `build_tool_install_preview`, `dashboard_server.py` scanner/catalog payloads, `dashboard-ui/`.
