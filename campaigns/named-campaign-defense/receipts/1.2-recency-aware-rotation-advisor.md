# Step 1.2 Receipt - Recency-Aware Rotation Advisor

Implemented the local-only recency and rotation-advice slice for IOC Watch.

## Delivered

- Added `security_observatory.recency` with read-only install evidence probing for recent `node_modules`, lockfile, npm log, pnpm, pip, and uv cache signals.
- Added repo-local rotation-surface enumeration for `.env*`, `.envrc`, project `.npmrc` / `.pypirc`, workflow `secrets.*` references, MCP config, Wrangler/Vercel env config, and project-local AWS/SSH config paths.
- Enriched IOC findings with `install_recency_confidence`, `last_install_signal_at`, evidence summaries, and enumerated rotation surfaces.
- Persisted those fields in SQLite findings so exported scans and dashboard cases can reference them.
- Upgraded IOC case copy and fix steps:
  - strong recency says "Probably executed" and recommends rotating enumerated repo-specific surfaces
  - weak/unknown recency stays softer and does not recommend rotation
- Added the same rotation section to the AI handoff Markdown prompt with provider-first/local-last/no-commit guardrails.
- Added `security-scan handoff <scan-id> --dry-run` for inspecting the generated handoff prompt without writing a file.
- Added a dashboard case-card rotation section for strong-recency IOC cases with enumerated surfaces.
- Rebuilt bundled dashboard assets.

## Verification

- `python3 -c "import sys; sys.path.insert(0, 'src'); import security_observatory.cli; print('ok')"` passed.
- `python3.13 -m pytest tests/test_iocs.py tests/test_ioc_recency.py tests/test_cases.py tests/test_dashboard_report_exports.py` passed: 30 tests.
- `python3.13 -m pytest` passed: 101 tests.
- `cd dashboard-ui && npm run lint` passed.
- `make dashboard-build` passed.

## Notes

- Default `python3` on this machine does not have pytest installed, so Python test verification used `python3.13`, which has the repo-compatible pytest environment.
- The probe does not invoke package managers, install tools, or make network calls.
- Surface enumeration stores paths only; test fixtures assert workflow secret names and placeholder values do not appear in rendered output.
