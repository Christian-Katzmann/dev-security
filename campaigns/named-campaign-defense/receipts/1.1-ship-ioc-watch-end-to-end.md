# Step 1.1 Receipt — Ship IOC Watch end-to-end

Implemented IOC Watch as a local-first named-campaign defense slice.

## Delivered

- Added `security_observatory.iocs` with YAML pack loading, malformed-pack issue reporting, starter pack discovery, exact package matching, namespace-prefix matching, and local domain evidence matching.
- Added SQLite `ioc_packs` and `ioc_indicators` tables with idempotent reimport and DB round-trip loading.
- Wired IOC Watch into default scan assembly after SBOM loading so matches become normal findings and cases in the existing lifecycle.
- Added `security-scan ioc [TARGET] [--feed=<file-or-dir>] [--all-repos] [--dev-root=<path>] [--json] [--fail-on=<severity>]`.
- Added built-in starter pack `src/security_observatory/iocs/starter/2026-05-12-supply-chain-worm.yaml`.
- Added `supply-chain-ioc` case copy, fix steps, and evidence fields.
- Added dashboard/report surfacing for named-campaign matches, including affected package, match type, pack/source, advisory, and evidence path.
- Added `docs/iocs.md` plus README usage examples.
- Added tests for pack loading, malformed packs, duplicate import, empty directories, exact match, no match, namespace hit, domain hit, CLI single repo, CLI all repos, JSON output, and `--fail-on`.

## Verification

- `python3 -c "import sys; sys.path.insert(0, 'src'); import security_observatory.cli; print('ok')"`: passed.
- `pytest`: 96 passed.
- `cd dashboard-ui && npm run lint`: passed.
- `make dashboard-build`: passed.
- Temporary local dashboard browser check on `127.0.0.1:8799`: Reports view rendered the Named-campaign matches panel with exact and domain rows; console had no errors.

## Notes

- IOC matching is offline. Default behavior loads local starter packs and does not call registries, advisory APIs, or package managers.
- Exact matches are critical. Namespace and domain watches are high-confidence-to-inspect signals, but intentionally not proof of execution.
