# Step 2.1 Receipt - Install Hook and Workflow Surface Scanners

## Delivered

- Added two built-in, read-only scanners:
  - `install-hooks` for npm install scripts, Python `pyproject.toml` build hooks, and `setup.py` install commands.
  - `workflow-audit` for GitHub Actions pins, fetch-and-exec blocks, secret handling, `pull_request_target` fork checkout, untrusted event text in shell, and broad token permissions.
- Added shared project-local allow-list handling:
  - `.devsec/install-hook-allowlist.yaml`
  - `.devsec/workflow-allowlist.yaml`
  - Entries require `rule`, `path`, and `reason`; missing reasons do not silence findings.
- Pre-populated the known-good Typst installer example:
  - `/Users/christiankatzmann/Dev/Projects/Obedai_JobManagement/.devsec/workflow-allowlist.yaml`
- Normalized actionable findings into existing `Finding` and case flow:
  - High/critical install hooks become `install-hooks` cases.
  - Workflow audit findings become `workflow` cases and appear in the IaC dashboard surface.
- Updated docs:
  - `docs/install-hooks.md`
  - `docs/workflow-audit.md`
  - `docs/scanners.md`
  - `docs/adding-scanners.md`
- Updated dashboard labels, scanner catalog fallback data, and the IaC view workflow count.

## Verification

- `python3 -c "import sys; sys.path.insert(0, 'src'); import security_observatory.cli; print('ok')"` passed.
- `python3.13 -m pytest tests/test_surface_scanners.py tests/test_scanners.py tests/test_normalize.py tests/test_cases.py` passed: 18 tests.
- `npm run lint` passed in `dashboard-ui`.
- `python3.13 -m pytest` passed: 105 tests.
- `make dashboard-build` passed and refreshed bundled dashboard assets.

## Notes

- The default `python3` is Python 3.14 and has no `pytest`; the full suite was run with `python3.13`, which has the repo's working pytest environment.
- No package managers, scanners, workflows, or networked security tools are invoked by the new adapters. They read local files and write JSON reports only.
