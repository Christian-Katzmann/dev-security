# Batch: 19-adx-and-docs-truth

## Purpose
DëvSec is a security tool branded on honesty, yet the repo's own account of itself has drifted behind its code: between 2026-05-21 and 2026-05-31 a guarded AI case-resolution / fix-proposal write subsystem landed (`mcp_server.py`, `fix_proposals.py`, `case_followup.py`, `decisions.py`), but the `.adx` agent-guidance contracts and several prose docs were frozen before it existed — and in two places they assert things that are provably false (pytest "can't run"; the MCP adapter is "read-only … stdio-only"; the rw README "adds three case-resolution tools only"). This batch fixes eight super-list items on one shared fix surface — **the repo's self-description (`.adx/*` + AGENTS.md + `mcp/README.md` + scanner/CLI docs)** — so a cold-reading agent and a human reader both get a true picture: **S-030** (refresh the `.adx` module map for the MCP write subsystem), **S-031** (make `.adx` safety/recovery/verification tell the truth + bump the stamp), **S-048** (fix the AGENTS.md MCP write-mode understatement), **S-049** (refresh the stale `.adx` pytest-blocked caveat), **S-050** (canonical-vs-working-notes doc boundary in AGENTS.md), **S-010** (correct the `mcp/README` write-surface tool count), **S-051** (document the security-sensitive CLI verbs/flags), and **S-052** (line-match destructive-surface doc claims to their guards). No source code or report is edited; the deliverable is documentation/manifest truth, anchored to real code line numbers.

## Source Evidence
- **S-030** — Refresh the `.adx` module map to include the MCP / fix-proposal / case-followup / decisions write subsystem (add a `mcp-write-surface` entry with `key_files`, matching tests, the new risk id, and a one-line pointer to `mcp/README.md` for the boundary) · evidence: `.adx/modules/index.json` lists 5 modules (`python-cli-scanners`, `dashboard-server`, `dashboard-ui`, `desktop-launcher`, `docs-and-templates`); a full `.adx` scan finds the symbols `fix_proposals`/`mcp_server`/`land_fix`/`propose_fix` in **0** `.adx` files and `devsec-mcp-rw` only in `commands.json`; the live modules `src/security_observatory/mcp_server.py` (~1,118 LOC), `fix_proposals.py` (~775), `case_followup.py` (~673), `decisions.py` (~425) and the whole `mcp/` dir are absent from the map · synthesis row S-030, lens report 16-ai-maintainability-health.initial.md (Rank 1, RH.1, US.1/US.2, TR.1).
- **S-031** — Make the `.adx` safety/recovery/verification contracts tell the truth: add a `mcp-write-surface` risk-register entry + add `devsec-mcp-rw`/`land_fix`/`propose_fix` to `dangerous_command_patterns`; rewrite `recovery.md`'s "Pytest Is Missing" + `verification.json`'s "currently blocked" note (pytest runs green); bump `adx.json` `last_verified`; add one AGENTS.md line mapping "Security Observatory" ↔ "DëvSec" · evidence: `.adx/risks.json` has 5 risk ids (`scanner-installer`, `local-security-data`, `honey-keys`, `desktop-process-control`, `generated-assets`) and a `dangerous_command_patterns` list with no `devsec-mcp-rw`/`land_fix`/`propose_fix`; `.adx/recovery.md:22-24` "Pytest Is Missing" says `python3 -m pytest --version` and `.venv/bin/python -m pytest --version` "both fail because pytest is not installed"; `.adx/verification.json:40` note "currently blocked until pytest exists in the selected Python environment"; `.adx/adx.json:34` `last_verified: 2026-05-12T13:35:03Z` (stale vs code through 2026-05-31); AGENTS.md uses "DëvSec" only in the MCP line · synthesis row S-031, lens report 16-ai-maintainability-health.initial.md (Rank 2, Rank 3, Rank 4, Rank 6, RH.2/RH.3/RH.4/RH.6, TR.2/TR.3).
- **S-048** — Fix the AGENTS.md MCP write-mode understatement: name both `devsec-mcp` (read-only default) and `devsec-mcp-rw` (guarded write — `case_resolutions.v1`, high/critical suppression held for human confirmation, clean-room diff-only reviewer), or explicitly defer to `mcp/README.md` · evidence: `AGENTS.md:41` reads "The MCP adapter (`devsec-mcp`) exposes read-only access to scan results. It is stdio-only and does not open a network port." while `pyproject.toml` ships both `devsec-mcp` and `devsec-mcp-rw` entry points, `README.md:43` and the full `mcp/README.md` "Guarded write mode" section both document the write path accurately; AGENTS.md is the one canonical doc denying a capability that ships · synthesis row S-048, lens report 15-documentation-health.initial.md (Rank 1, RH.1, US.1, TR.1) and 04-permission-boundary-health.initial.md (Undocumented Surfaces #1).
- **S-049** — Refresh the stale `.adx` pytest-blocked verification caveat so AGENTS.md and the matrix agree · evidence: `.adx/verification.json:40` note "The pytest command is authoritative but currently blocked until pytest exists in the selected Python environment"; contradicted by `pyproject.toml` `[dependency-groups]` `pytest>=9.0.3`, 47 `tests/test_*.py` files, `.adx/commands.json` shipping `python-dev-sync` + `python-pytest`, and AGENTS.md's Verification section unconditionally saying "run `uv run pytest`"; live confirmation this session: `uv run pytest -q` = **409 passed, 4 skipped** · synthesis row S-049, lens report 15-documentation-health.initial.md (Rank 2, RH.2, US.4, TR.2).
- **S-050** — Add a canonical-vs-working-notes doc boundary in AGENTS.md "Start Here": mark `campaigns/`, `reports/campaign-automation/`, and root scratch docs (`next-step.md`, `overview-redesign-*.md`) as historical/working-notes, not contract · evidence: the repo has 254 `.md` files (most are campaign plans/receipts + automation timelines); `campaigns/` and `reports/campaign-automation/` exist; root scratch docs `next-step.md`, `overview-redesign-diff-grounded.md`, `overview-redesign-model-split.md`, `overview-redesign-prompts.md` exist; `AGENTS.md:3` "Start Here" names the canonical `.adx` files but does not demote the churn · synthesis row S-050, lens report 15-documentation-health.initial.md (Rank 3, RH.3/RH.7/RH.8, US.5, TR.3).
- **S-010** — Correct the `mcp/README` write-surface tool count (3 → 8): update the README tool table and the "write mode is case-only" framing to match the registered tool set · evidence: `mcp/README.md:21` "The write-enabled adapter adds three case-resolution tools only" and `mcp/README.md:50,58` "the `tools/list` response should list eleven tools … plus three", but `src/security_observatory/mcp_server.py` registers **8** write-mode tools — `trigger_scan` (:911), `case_followup_prompt` (:930), `preview_case_resolutions` (:953), `apply_case_resolutions` (:976), `propose_fix` (:1001), `clean_room_review_packet` (:1036), `record_clean_room_review` (:1051), `land_fix` (:1079); the in-code `_WRITE_MODE_BOUNDARY` instruction string is accurate · synthesis row S-010, lens report 04-permission-boundary-health.initial.md (Undocumented Surfaces #1, US.1, TR.3) and 15-documentation-health.initial.md (Rank 1 cross-ref).
- **S-051** — Document (or deliberately note as code-only) the security-sensitive aliased CLI verbs and suppressed flags that gate destructive ops · evidence: `cli.py` dispatch handles aliased verbs `reset`/`factory-reset`, `case-decision`/`resolve-cases`, `propose-fix`/`land-fix`, `review-fix`/`clean-room-review`, `vex-import`/`vex-export`, most absent from README Usage; `cli.py:85-96` hides via `argparse.SUPPRESS` the flags `--action`, `--scope`, `--case-id`, `--preview`, `--apply`, `--confirm-suppression`, `--include-rotation-scaffold`, `--backup-to`, `--yes` — and `--confirm-suppression`/`--apply`/`--yes`/`--backup-to` gate destructive/irreversible behavior, with guard semantics living only in code (e.g. the `--confirm-suppression` prompt at `cli.py:722`) · synthesis row S-051, lens report 15-documentation-health.initial.md (Rank 6, US.2/US.3, "Undocumented Or Hidden Surfaces" rows 2–3) and feature US.4/US.5 (merged into S-051).
- **S-052** — Line-match the documented destructive-surface (Honey Key) safety claims to concrete guards in code · evidence: README and `docs/honey-keys.md` claim overwrite refusal, in-repo placement, insertion confirmation, and hash-only storage; the guards are real and locatable — overwrite refusal `target_path.exists()` → 409 "Placement file already exists" at `dashboard_server.py:2772-2773`, in-repo containment `target_path.relative_to(repo_path)` at `:2765` (and key-must-match-repo "belongs to a different repo" at `:2787`), pre-insert `409 "Honey Key already exists."` at `:2675`, hash-only storage `hash_honey_key` → `hashlib.sha256` at `honey_keys.py:85-86` (token stored as `token_hash`, `:41`/`:57`); the doc pass could not complete this line-match (channel outage), so S-052 records the proof binding each claim to its guard · synthesis row S-052, lens report 15-documentation-health.initial.md (Rank 4, RH.4/RH.5, TR.3) and 04-permission-boundary-health.initial.md (Rank 6).

## Target
Move S-030, S-031, S-048, S-049, S-050, S-010, S-051, S-052 from Yellow (S-030, S-031, S-049, S-050 are the worst, at Yellow; S-048, S-010, S-051, S-052 are Green/Yellow) to Green.

## Dependencies
None. Every row's Dependencies column in the health matrix is `—`. These are all documentation/manifest edits with no code coupling, so they can land in any order and in parallel. One soft ordering only: do **S-031** (rewrite the recovery/verification pytest notes after re-running `uv run pytest`) and **S-049** (refresh the same `.adx/verification.json` caveat) together, since both edit the pytest-truth claim — confirm the live pass/skip count once, then write both notes from that single observation so they cannot disagree.

## Non-Goals
- Do not attempt other batches' super-list items (the dashboard split S-016, scanner registry S-018, type floor S-021, CHANGELOG/version S-046, and KEV/EPSS wiring S-008 are separate batches; in particular, the runtime CSRF fix for the dashboard mutating surface is S-001 / batch 02 — here only the *documentation* of the threat surface is in scope).
- Do not broaden this into a general cleanup.
- Do not make production, destructive, deploy, secret, or irreversible data changes without explicit approval.
- **Documentation/manifest only:** do not edit any `src/` code, any test, any report under `reports/`, or the generated dashboard assets to make a doc claim true — every claim in this batch is already true in code; the fix is to make the docs describe it accurately. (If a doc claim is found to be false in code, stop and flag it rather than silently "fixing" prose to hide a real gap.)
- Do not delete or relocate the campaign/automation/scratch docs in S-050; only demote them in AGENTS.md "Start Here" (relocating root scratch docs under `docs/notes/` is optional and out of scope unless trivially safe).
- Do not re-introduce a hardcoded "317" or "467" test count: state the count you actually observe from `uv run pytest -q` this run (live: 409 passed, 4 skipped).
- Do not rewrite the in-code `_WRITE_MODE_BOUNDARY` string or `mcp/README.md`'s already-accurate "Guarded write mode" prose — S-010 corrects only the tool *count* and the "three tools only" framing.

## Suggested Starting Steps
1. Re-read this context and acceptance.md.
2. Re-verify each S-ID's evidence against the exact files cited: `.adx/modules/index.json` (5 modules, no MCP), `.adx/risks.json` (5 risk ids, `dangerous_command_patterns` with no rw verbs), `.adx/recovery.md:22-24`, `.adx/verification.json:40`, `.adx/adx.json:34`, `AGENTS.md:41` + `:3` "Start Here", `mcp/README.md:21/50/58` vs `mcp_server.py:911-1079`, `cli.py:85-96`/`:722`, and the honey guards at `dashboard_server.py:2675/2765/2772-2773/2787` + `honey_keys.py:41/57/85-86`.
3. Run `uv run pytest -q` once and record the observed pass/skip line; use that single number to rewrite **S-031**'s `recovery.md` "Pytest Is Missing" section and `verification.json`'s "currently blocked" note, and to close **S-049**'s caveat. Inspect the 4 skipped tests so the note is honest about *why* they skip (likely the `mcp`-extra gate, owned by batch 05 — do not fix them here, just describe accurately).
4. **S-030 / S-031 manifest edits:** add a `mcp-write-surface` module entry to `.adx/modules/index.json` (+ a `modules/mcp-write-surface.md`) listing `mcp_server.py`, `mcp/`, `fix_proposals.py`, `case_followup.py`, `decisions.py` with `key_files`, the matching tests (`test_mcp_server.py`, `test_fix_proposals.py`, `test_mcp_fix_proposals.py`, `test_case_followup.py`, `test_mcp_trigger_scan.py`, `test_red_team_e2e.py`), and a one-line pointer to `mcp/README.md`; add a matching `mcp-write-surface` risk entry to `.adx/risks.json` plus `devsec-mcp-rw`/`land_fix`/`propose_fix` to `dangerous_command_patterns`; after the above land, bump `.adx/adx.json` `last_verified` to a timestamp newer than the newest source mtime. Validate every edited JSON parses.
5. **Prose edits:** rewrite `AGENTS.md:41` to name both adapters (or defer to `mcp/README.md`) and add one "Security Observatory ↔ DëvSec" line plus the canonical-vs-working-notes demotion in "Start Here" (S-048, S-031, S-050); correct the three tool-count claims in `mcp/README.md` (S-010); add a short "security-sensitive CLI verbs & flags" subsection to the appropriate doc (README or `docs/` CLI reference) covering the aliases and suppressed flags, or deliberately note them code-only (S-051); add a doc-to-guard fidelity note (or an inline citation table) line-matching each Honey Key claim to its guard at the line numbers above (S-052).
6. Implement the smallest root-cause edit that satisfies every acceptance criterion; after the edits, re-run the fast import check and `uv run pytest` to confirm nothing in `src/` was touched and the suite still passes, and `json.load` every edited `.adx/*.json`.
