# Acceptance: 19-adx-and-docs-truth

## Acceptance Criteria
- **S-030 (module map covers the MCP write subsystem):** `.adx/modules/index.json` now contains an entry (e.g. `mcp-write-surface`) whose `key_files` name `mcp_server.py`, `fix_proposals.py`, `case_followup.py`, and `decisions.py`, with the matching tests and a one-line pointer to `mcp/README.md` for the boundary. `python3 -c "import json;json.load(open('.adx/modules/index.json'))"` parses, and every `key_files` path listed for the new entry exists (`ls` each resolves). `grep -rl "mcp_server\|fix_proposals\|land_fix\|propose_fix" .adx/` now returns at least the module map (was 0 before the batch).
- **S-031a (risk register + danger patterns cover the write surface):** `.adx/risks.json` has a new risk entry (e.g. `mcp-write-surface`) referencing the guarded write path (`mcp_server.py`, `fix_proposals.py`, `case_followup.py`, `decisions.py`) and its `dangerous_command_patterns` list now includes `devsec-mcp-rw`, `land_fix`, and `propose_fix`. `python3 -c "import json;json.load(open('.adx/risks.json'))"` parses; the new entry's paths reference real files.
- **S-031b (recovery/verification stop asserting pytest can't run):** `.adx/recovery.md`'s "Pytest Is Missing" section and `.adx/verification.json`'s "currently blocked until pytest exists" note are rewritten to state that `uv run pytest` runs, citing the count observed this run (`409 passed, 4 skipped` — confirm live and use the observed line, never the stale `317`/`467`); the rewrite describes *why* the 4 tests skip (do not change those tests). `grep -ri "pytest is missing\|currently blocked\|pytest is not installed" .adx/` returns nothing.
- **S-031c (verification stamp is current):** After the S-030/S-031a/b edits land, `.adx/adx.json` `last_verified` is bumped to a timestamp newer than the newest source-file mtime. `python3 -c "import json,os,glob;d=json.load(open('.adx/adx.json'));lv=d['last_verified'];newest=max(os.path.getmtime(p) for p in glob.glob('src/security_observatory/**/*.py',recursive=True));import datetime;print(lv, datetime.datetime.fromtimestamp(newest).isoformat())"` shows `last_verified` no longer `2026-05-12T13:35:03Z` and not older than the newest source mtime.
- **S-031d (brand identity mapped):** `AGENTS.md` contains one line stating Security Observatory is the package/repo name and DëvSec is the product brand. `grep -in "security observatory" AGENTS.md` and `grep -in "dëvsec\|devsec" AGENTS.md` both resolve to a line that equates the two.
- **S-048 (AGENTS.md MCP line is honest):** `AGENTS.md:41` no longer describes the adapter as read-only only. It names both `devsec-mcp` (read-only default) and `devsec-mcp-rw` (guarded write — `case_resolutions.v1`, high/critical suppression held for human confirmation, clean-room diff-only reviewer), or explicitly defers to `mcp/README.md` for the write mode. The named entry points match `pyproject.toml` `[project.scripts]` (`devsec-mcp`, `devsec-mcp-rw`). `grep -n "read-only access to scan results" AGENTS.md` returns nothing (the understating sentence is gone or qualified).
- **S-049 (stale pytest caveat removed):** `.adx/verification.json` no longer claims pytest is "currently blocked"; its note matches AGENTS.md's unconditional "run `uv run pytest`" (with `uv sync --dev` named only as a stale-env contingency). The note's claim is reproducible: `uv sync --dev && uv run pytest -q` runs green from this checkout.
- **S-050 (canonical-vs-working-notes boundary):** `AGENTS.md` "Start Here" now marks `campaigns/`, `reports/campaign-automation/`, and the root scratch docs (`next-step.md`, `overview-redesign-*.md`) as historical/working-notes — not contract. `grep -niE "historical|working.notes|not contract|superseded" AGENTS.md` resolves to a section naming those paths.
- **S-010 (MCP README write-surface count corrected):** `mcp/README.md` no longer says the write adapter "adds three case-resolution tools only" or that `tools/list` is "eleven … plus three." The tool table/count matches the 8 write-mode tools registered in `mcp_server.py` (`trigger_scan`, `case_followup_prompt`, `preview_case_resolutions`, `apply_case_resolutions`, `propose_fix`, `clean_room_review_packet`, `record_clean_room_review`, `land_fix`). `grep -n "three case-resolution tools only\|plus three" mcp/README.md` returns nothing; the README's listed write tools equal the `grep -nE "def (trigger_scan|case_followup_prompt|preview_case_resolutions|apply_case_resolutions|propose_fix|clean_room_review_packet|record_clean_room_review|land_fix)\b" src/security_observatory/mcp_server.py` set (8).
- **S-051 (security-sensitive CLI verbs/flags documented):** The aliased verbs (`reset`/`factory-reset`, `case-decision`/`resolve-cases`, `propose-fix`/`land-fix`, `review-fix`/`clean-room-review`, `vex-import`/`vex-export`) and the suppressed destructive flags (`--confirm-suppression`, `--apply`, `--yes`, `--backup-to`) are now covered in prose (README or a `docs/` CLI reference), or each is deliberately and explicitly noted as code-only with its guard. The documented verbs/flags match what `cli.py` exposes (cross-checked against `python -m security_observatory.cli --help` for the visible surface and `cli.py:85-96` for the suppressed flags). `grep -riE "confirm-suppression|factory-reset|land-fix|vex-export" docs/ README.md` resolves to the new prose.
- **S-052 (Honey Key doc claims line-matched to guards):** A doc-to-guard fidelity note (or citation table) now binds each documented Honey Key safety claim to a concrete guard: overwrite refusal → `dashboard_server.py:3379-3380` (`target_path.exists()` → 409); in-repo placement → `dashboard_server.py:3372` (`target_path.relative_to(repo_path)`); pre-insert duplicate refusal → `dashboard_server.py:3282` (409 "Honey Key already exists."); hash-only storage → `honey_keys.py:85-86` (`hash_honey_key` / `hashlib.sha256`, stored as `token_hash`). Each cited line still contains the asserted guard (verified by reading the code, not by editing it).
- **No-regression (whole batch):** No file under `src/`, `tests/`, or `reports/` is modified; the only changes are under `.adx/`, `AGENTS.md`, `mcp/README.md`, and the chosen CLI/docs surface. The fast import check passes and `uv run pytest` is still green after the batch, proving the edits were documentation-only.

## Required Checks
| Check | Why |
| --- | --- |
| `python3 -c "import json;json.load(open('.adx/modules/index.json'));json.load(open('.adx/risks.json'));json.load(open('.adx/adx.json'))"` | Proves S-030/S-031: every edited `.adx` manifest still parses as valid JSON. |
| `ls` each `key_files` path in the new module-map entry; `grep -rl "mcp_server\|fix_proposals\|land_fix\|propose_fix" .adx/` | Proves S-030: the MCP write subsystem is now named in the map and its files are real (was 0 `.adx` hits before). |
| `grep -i "devsec-mcp-rw\|land_fix\|propose_fix" .adx/risks.json` returns matches | Proves S-031a: the rw verbs are in `dangerous_command_patterns` and the risk register now cross-references the write surface. |
| `uv run pytest -q` (record the pass/skip line) | Proves S-031b/S-049: pytest actually runs — the rewritten recovery/verification notes cite the real, observed count (live: 409 passed, 4 skipped), not the stale "blocked"/"317"/"467". |
| `grep -ri "pytest is missing\|currently blocked\|pytest is not installed" .adx/` returns nothing | Proves S-031b/S-049: the false "pytest can't run" claims are gone from both `recovery.md` and `verification.json`. |
| `python3 -c "import json;print(json.load(open('.adx/adx.json'))['last_verified'])"` shows a value newer than `2026-05-12T13:35:03Z` and not older than the newest `src/**.py` mtime | Proves S-031c: the verification stamp matches the current code instead of granting false confidence in stale contracts. |
| `grep -n "read-only access to scan results" AGENTS.md` returns nothing; `grep -n "devsec-mcp-rw" AGENTS.md` returns a match (or AGENTS.md defers to `mcp/README.md`) | Proves S-048: the canonical agent guide no longer denies the shipped guarded write path. |
| `diff` of the `mcp/README.md` write-tool list against `grep -nE "def (trigger_scan\|case_followup_prompt\|preview_case_resolutions\|apply_case_resolutions\|propose_fix\|clean_room_review_packet\|record_clean_room_review\|land_fix)\b" src/security_observatory/mcp_server.py` (8 tools); `grep -n "three case-resolution tools only\|plus three" mcp/README.md` returns nothing | Proves S-010: the README write-surface count (8) matches the registered tools and the "three only" understatement is gone. |
| `grep -niE "historical\|working.notes\|not contract\|superseded" AGENTS.md`; `grep -ni "security observatory" AGENTS.md` | Proves S-050 + S-031d: AGENTS.md demotes campaign/automation/scratch docs and equates the Security Observatory / DëvSec identities. |
| `python -m security_observatory.cli --help`; `grep -riE "confirm-suppression\|factory-reset\|land-fix\|vex-export" docs/ README.md` | Proves S-051: the security-sensitive verbs/flags are now covered in prose (or explicitly noted code-only) and the prose matches the real CLI surface. |
| Read `dashboard_server.py:3282/3372/3379-3380/3394` and `honey_keys.py:85-86`; confirm each cited guard still exists | Proves S-052: every documented Honey Key safety claim is line-matched to a guard that actually enforces it. |
| `python3 -c "import sys; sys.path.insert(0, 'src'); import security_observatory.cli; print('ok')"`; `git status --porcelain src tests reports` shows no changes | Fast import check + proves the no-regression criterion: the batch touched only docs/manifests, not code, tests, or reports. |

## Receipt
When complete, write a receipt using:
`/Users/christiankatzmann/Dev/skills/codebase-health-kit/templates/implementation-receipt.md`
Save it under this plan's `receipts/` directory.
