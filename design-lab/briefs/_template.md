# Brief — <Screen name>

> One page, filled before any pixels. Copy to `briefs/<id>.md`.
> The point is to design the screen, not port the old one.

## Who's here & why
<!-- The actual person, their state of mind, the job they came to do.
     e.g. "An engineer who just ran a scan and wants to know: am I okay,
     and if not, what's the one thing to fix first?" -->

## The one thing
<!-- The single primary action or the single verdict this screen must surface
     first. If you can't name one, the screen is doing too much. -->

## Keep / cut / merge  (from the feature map)
<!-- Go through the live screen's feature map (app.jsx → the section's `map`).
     Decide each one. Keeping everything is not the goal. -->

| Feature (from today) | Keep / Cut / Merge | Why |
|---|---|---|
|  |  |  |

## What's missing
<!-- What the new screen should give the user that the old one never did.
     This is where UX-first beats reskin. -->

## Layout sketch
<!-- A few lines: the regions, the hierarchy, what's above the fold, what's
     progressive disclosure. Where (if anywhere) the one accent color earns a place. -->

## Gate (check before Park)
- [ ] **Brand** — reads as the new dark language (mood, mono labels, hairline borders, sparse accent)
- [ ] **UX** — the one thing is obvious; cognitive load is low; empty/loading/error states considered
- [ ] **Coverage** — every "keep" from the map is present
