# Journeys

Mode: quick --pack launch-readiness
Plan approval hash: not required for local read-only quick run

- dashboard-overview-renders: Open `/`, confirm the main shell and posture gauge render, and capture a screenshot. (read-only)
- dashboard-summary-api-responds: GET `/api/summary` must return 200. (read-only)
- dashboard-tool-catalog-api-responds: GET `/api/tool-catalog` must return 200. (read-only)
- dashboard-activity-renders: Open Activity and confirm activity charts render. (read-only)
- dashboard-cases-renders: Open Cases and confirm the risk landscape renders. (read-only)
- dashboard-catalog-renders: Open Tool catalog and confirm catalog content renders. (read-only)
- dashboard-reports-renders: Open Reports and confirm saved report inventory renders. (read-only)
- dashboard-settings-renders: Open Settings and confirm privacy/storage controls render. (read-only)
